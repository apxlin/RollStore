import "./mmrRollUp512" as mmrRollUp512

def main(field root, field width, field[512] xItems, field[512] yItems, field newRoot, private field[16] xPeaks, private field[16] yPeaks) -> (field):
    newRoot == mmrRollUp512(root, width, xPeaks, yPeaks, xItems, yItems)
    return 1
